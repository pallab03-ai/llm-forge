# Inference service

Phase 0 placeholder. Will contain a FastAPI-based inference server
that loads a base model + LoRA adapter from MinIO and exposes
an OpenAI-compatible `/v1/chat/completions` endpoint.
