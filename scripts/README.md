# Operational scripts

Phase 0 placeholder. Future scripts will include:

- `seed_db.py` — populate reference data
- `bootstrap_buckets.py` — create MinIO buckets
- `run_migrations.sh` — apply Alembic migrations
